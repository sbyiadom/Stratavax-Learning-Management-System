'use client'

// Simple template to help Next.js generate client manifest
// No external dependencies required
export default function DashboardTemplate({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
