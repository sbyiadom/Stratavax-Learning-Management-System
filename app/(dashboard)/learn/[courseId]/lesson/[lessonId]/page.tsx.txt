'use client'

import { useState } from 'react'
import { useParams, useRouter } from 'next/navigation'
import { 
  ArrowLeft, 
  ArrowRight, 
  CheckCircle, 
  PlayCircle,
  FileText,
  Download,
  MessageSquare,
  BookOpen,
  Clock,
  Star
} from 'lucide-react'
import Link from 'next/link'

// Mock lesson data
const mockLesson = {
  id: 1,
  title: 'React Hooks Fundamentals',
  courseId: 1,
  description: 'Learn about useState, useEffect, and other essential React hooks that revolutionized functional components.',
  duration: '45 minutes',
  content: `
    # React Hooks Fundamentals

    React Hooks were introduced in React 16.8 and allow you to use state and other React features without writing a class.

    ## useState Hook

    The \`useState\` hook lets you add state to functional components.

    \`\`\`jsx
    import React, { useState } from 'react';

    function Counter() {
      const [count, setCount] = useState(0);

      return (
        <div>
          <p>You clicked {count} times</p>
          <button onClick={() => setCount(count + 1)}>
            Click me
          </button>
        </div>
      );
    }
    \`\`\`

    ## useEffect Hook

    The \`useEffect\` hook lets you perform side effects in functional components.

    \`\`\`jsx
    import React, { useState, useEffect } from 'react';

    function Timer() {
      const [seconds, setSeconds] = useState(0);

      useEffect(() => {
        const interval = setInterval(() => {
          setSeconds(seconds => seconds + 1);
        }, 1000);

        return () => clearInterval(interval);
      }, []);

      return <div>Timer: {seconds} seconds</div>;
    }
    \`\`\`

    ## Key Points to Remember

    1. **Only Call Hooks at the Top Level**
       - Don't call hooks inside loops, conditions, or nested functions

    2. **Only Call Hooks from React Functions**
       - Call hooks from React function components or custom hooks

    3. **Custom Hooks**
       - You can create your own hooks to reuse stateful logic
  `,
  videoUrl: 'https://example.com/video.mp4',
  resources: [
    { name: 'Lesson Slides', type: 'pdf', size: '2.1 MB' },
    { name: 'Code Examples', type: 'zip', size: '5.3 MB' },
    { name: 'Official Documentation', type: 'link', url: 'https://reactjs.org/docs/hooks-intro.html' }
  ],
  completed: false,
  nextLessonId: 2,
  prevLessonId: null
}

export default function LessonPage() {
  const params = useParams()
  const router = useRouter()
  const [isCompleted, setIsCompleted] = useState(mockLesson.completed)
  const [showNotes, setShowNotes] = useState(false)
  const [notes, setNotes] = useState('')

  const handleMarkComplete = () => {
    setIsCompleted(true)
    // In real app, would update progress in database
    console.log('Lesson marked as complete')
  }

  const handleNextLesson = () => {
    if (mockLesson.nextLessonId) {
      router.push(`/learn/${params.courseId}/lesson/${mockLesson.nextLessonId}`)
    }
  }

  const handlePrevLesson = () => {
    if (mockLesson.prevLessonId) {
      router.push(`/learn/${params.courseId}/lesson/${mockLesson.prevLessonId}`)
    } else {
      router.push(`/learn/${params.courseId}`)
    }
  }

  return (
    <div className="min-h-screen bg-gray-50">
      {/* Lesson Header */}
      <div className="bg-white border-b">
        <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center space-x-4">
              <button
                onClick={handlePrevLesson}
                className="flex items-center text-gray-600 hover:text-gray-900"
              >
                <ArrowLeft className="h-5 w-5 mr-1" />
                Back to Course
              </button>
              <div className="h-6 w-px bg-gray-300" />
              <div>
                <h1 className="text-xl font-bold text-gray-900">{mockLesson.title}</h1>
                <div className="flex items-center text-sm text-gray-600 mt-1">
                  <Clock className="h-3 w-3 mr-1" />
                  <span>{mockLesson.duration}</span>
                  <span className="mx-2">•</span>
                  <BookOpen className="h-3 w-3 mr-1" />
                  <span>Lesson {params.lessonId}</span>
                </div>
              </div>
            </div>
            
            <div className="flex items-center space-x-4">
              {isCompleted ? (
                <div className="flex items-center text-green-600">
                  <CheckCircle className="h-5 w-5 mr-2" />
                  <span className="font-medium">Completed</span>
                </div>
              ) : (
                <button
                  onClick={handleMarkComplete}
                  className="flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 font-medium"
                >
                  <CheckCircle className="h-5 w-5 mr-2" />
                  Mark Complete
                </button>
              )}
            </div>
          </div>
        </div>
      </div>

      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-8">
          {/* Main Content */}
          <div className="lg:col-span-2 space-y-8">
            {/* Video Player */}
            <div className="bg-black rounded-xl overflow-hidden aspect-video">
              <div className="h-full flex items-center justify-center">
                <div className="text-center">
                  <PlayCircle className="h-16 w-16 text-white/50 mx-auto mb-4" />
                  <p className="text-white/70">Video content would play here</p>
                  <p className="text-white/50 text-sm mt-2">Demo: {mockLesson.videoUrl}</p>
                </div>
              </div>
            </div>

            {/* Lesson Content */}
            <div className="bg-white rounded-xl shadow-lg p-8">
              <div className="prose max-w-none">
                <h2 className="text-2xl font-bold mb-6">Lesson Content</h2>
                <div className="text-gray-700 whitespace-pre-wrap">
                  {mockLesson.content}
                </div>
              </div>
            </div>

            {/* Notes Section */}
            <div className="bg-white rounded-xl shadow-lg p-8">
              <div className="flex items-center justify-between mb-6">
                <h2 className="text-2xl font-bold">My Notes</h2>
                <button
                  onClick={() => setShowNotes(!showNotes)}
                  className="text-blue-600 hover:text-blue-800 font-medium"
                >
                  {showNotes ? 'Hide' : 'Take Notes'}
                </button>
              </div>
              
              {showNotes && (
                <div>
                  <textarea
                    value={notes}
                    onChange={(e) => setNotes(e.target.value)}
                    placeholder="Write your notes here..."
                    className="w-full h-48 p-4 border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
                  />
                  <div className="flex justify-end mt-4">
                    <button className="px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 font-medium">
                      Save Notes
                    </button>
                  </div>
                </div>
              )}
              
              {!showNotes && notes && (
                <div className="border rounded-lg p-4 bg-gray-50">
                  <p className="text-gray-700">{notes}</p>
                </div>
              )}
            </div>
          </div>

          {/* Sidebar */}
          <div className="space-y-8">
            {/* Resources */}
            <div className="bg-white rounded-xl shadow-lg p-6">
              <h3 className="font-bold text-lg mb-4 flex items-center">
                <FileText className="h-5 w-5 mr-2" />
                Resources
              </h3>
              <div className="space-y-3">
                {mockLesson.resources.map((resource, index) => (
                  <div key={index} className="border rounded-lg p-3 hover:bg-gray-50">
                    <div className="flex items-center justify-between">
                      <div>
                        <p className="font-medium">{resource.name}</p>
                        <p className="text-sm text-gray-500">
                          {resource.type.toUpperCase()} • {resource.size || 'Link'}
                        </p>
                      </div>
                      {'url' in resource ? (
                        <a
                          href={resource.url}
                          target="_blank"
                          rel="noopener noreferrer"
                          className="text-blue-600 hover:text-blue-800"
                        >
                          Open
                        </a>
                      ) : (
                        <button className="text-blue-600 hover:text-blue-800">
                          <Download className="h-4 w-4" />
                        </button>
                      )}
                    </div>
                  </div>
                ))}
              </div>
            </div>

            {/* Navigation */}
            <div className="bg-white rounded-xl shadow-lg p-6">
              <h3 className="font-bold text-lg mb-4">Lesson Navigation</h3>
              <div className="space-y-4">
                <button
                  onClick={handlePrevLesson}
                  disabled={!mockLesson.prevLessonId}
                  className={`w-full flex items-center justify-between p-3 rounded-lg border ${
                    mockLesson.prevLessonId
                      ? 'hover:bg-gray-50 text-gray-700'
                      : 'opacity-50 cursor-not-allowed'
                  }`}
                >
                  <div className="flex items-center">
                    <ArrowLeft className="h-4 w-4 mr-2" />
                    <div className="text-left">
                      <p className="text-sm text-gray-500">Previous</p>
                      <p className="font-medium">State and Props</p>
                    </div>
                  </div>
                </button>
                
                <button
                  onClick={handleNextLesson}
                  disabled={!mockLesson.nextLessonId}
                  className={`w-full flex items-center justify-between p-3 rounded-lg border ${
                    mockLesson.nextLessonId
                      ? 'hover:bg-gray-50 text-gray-700'
                      : 'opacity-50 cursor-not-allowed'
                  }`}
                >
                  <div className="text-left">
                    <p className="text-sm text-gray-500">Next</p>
                    <p className="font-medium">Context API</p>
                  </div>
                  <ArrowRight className="h-4 w-4 ml-2" />
                </button>
              </div>
              
              <div className="mt-6 pt-6 border-t">
                <Link
                  href={`/learn/${params.courseId}`}
                  className="block w-full text-center py-2 border border-blue-600 text-blue-600 rounded-lg hover:bg-blue-50 font-medium"
                >
                  Back to Course Overview
                </Link>
              </div>
            </div>

            {/* Discussion */}
            <div className="bg-white rounded-xl shadow-lg p-6">
              <h3 className="font-bold text-lg mb-4 flex items-center">
                <MessageSquare className="h-5 w-5 mr-2" />
                Discussion
              </h3>
              <p className="text-gray-600 mb-4">
                Have questions about this lesson? Join the discussion.
              </p>
              <button className="w-full py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 font-medium">
                Open Discussion
              </button>
            </div>

            {/* Progress */}
            <div className="bg-white rounded-xl shadow-lg p-6">
              <h3 className="font-bold text-lg mb-4">Your Progress</h3>
              <div className="space-y-4">
                <div>
                  <div className="flex justify-between text-sm mb-1">
                    <span className="text-gray-600">Course Progress</span>
                    <span className="font-semibold">45%</span>
                  </div>
                  <div className="w-full bg-gray-200 rounded-full h-2">
                    <div 
                      className="bg-blue-600 h-2 rounded-full"
                      style={{ width: '45%' }}
                    ></div>
                  </div>
                </div>
                
                <div>
                  <div className="flex justify-between text-sm mb-1">
                    <span className="text-gray-600">Lessons Completed</span>
                    <span className="font-semibold">3/8</span>
                  </div>
                  <div className="w-full bg-gray-200 rounded-full h-2">
                    <div 
                      className="bg-green-600 h-2 rounded-full"
                      style={{ width: '37.5%' }}
                    ></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  )
}